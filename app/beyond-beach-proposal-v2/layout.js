export const metadata = {
  title: 'Beyond Beach — Digital Platform Proposal v2',
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function ProposalLayout({ children }) {
  return children;
}
